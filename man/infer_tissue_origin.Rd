% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tissue_inference.R
\name{infer_tissue_origin}
\alias{infer_tissue_origin}
\title{Infer Tissue Origin from Cell Types and Marker Genes}
\usage{
infer_tissue_origin(
  seurat_obj,
  save_path_markers = NULL,
  save_path_rds = NULL,
  max_cells_per_cluster = 200,
  test_use = "wilcox"
)
}
\arguments{
\item{seurat_obj}{A Seurat object with cell type annotations}

\item{save_path_markers}{Character. Path to save the top marker gene CSV (default: NULL)}

\item{save_path_rds}{Character. Path to save the updated Seurat object (default: NULL)}

\item{max_cells_per_cluster}{Integer. Maximum cells to use per cluster for marker finding (default: 200)}

\item{test_use}{Character. Test to use for finding markers (default: "wilcox")}
}
\value{
A list containing the top markers, cell type summary, and tissue hypothesis
}
\description{
Based on dominant annotated cell types and top marker genes, proposes a tissue
of origin for the dataset. Saves top markers and final Seurat object.
}
\examples{
\dontrun{
results <- infer_tissue_origin(
  seurat_obj,
  save_path_markers = "data/top_markers.csv",
  save_path_rds = "results/seurat_analysis_complete.rds"
)
}

}
